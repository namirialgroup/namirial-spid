### Description

This project contains docs, notes and code related to SPID.

## Saml Response Validator
Validate the response returned from an IDP. Save the base64 response to a file and pass the path as an argument of the class SamlResponseValidator.java
